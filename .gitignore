# Node
.node_repl_history

# npm
node_modules/
package-lock.json

# - output of npm pack
*.tgz

# TypeScript
# - compiler output
dist

# Excel files are ignored in general because we'll
# likely make a lot of them when tinkering
*.xlsx
